name: bluetooth_mesh_test
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  nearby_connections: ^5.1.0  # 蓝牙自组网核心插件
  permission_handler: ^11.0.0 # 权限管理插件

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
